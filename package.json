{
  "name": "setae",
  "version": "0.0.1",
  "description": "CLI for interacting with setae backends",
  "bin": {
    "setae": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
